on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    name: Build C# binary
    steps:
      - uses: actions/setup-dotnet@v1.8.0
      - run: dotnet build <my project>